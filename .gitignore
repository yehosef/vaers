node_modules/
/vendor/
/data/
web/dist/

# local working notes (session state, code-review transcripts)
.claude/
*.duckdb
*.duckdb.wal
*.log
.DS_Store

# The ~560 MB all-years bundle is published as a GitHub Release asset, not stored
# in git (it would exceed GitHub's free LFS quota). Fetched by pipeline/setup.js.
datasets/AllVAERSDataCSVS.zip
datasets/AllVAERSDataCSVS.zip.part

# extracted from datasets/AllVAERSDataCSVS.zip by pipeline/setup.js
datasets/VAERS/data/
